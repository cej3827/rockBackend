components:
  schemas:
    Group:
      properties:
        member:
          type: array
          items:
            type: object
            properties:
              user_NAME:
                type: string
              color:
                type: string
              answer_Status:
                type: boolean
                description: 답변했는지 
          description: 닉네임, 컬러, 답변여부
        checkAll:
          type: boolean
          description: 모든 멤버가 답변했는지
        todayQuestion:
          type: object
          properties:
            Q_NUM:
              type: integer
            Q_DETAIL:
              type: string
          description: 오늘의 질문
    createGroup:
      properties:
        group_Name:
          type: string
        group_IMG:
          type: string
        cnt:
          type: integer
        user_ID:
          type: string
        color_Key:
          type: string
        invite_Code:
          type: null
    createMember:
      properties:
        group_ID:
          type: integer
        user_ID:
          type: string
        member_NAME:
          type: string
        answer_Status:
          type: boolean
        color:
          type: string
        group_IMG:
          type: string
    createGroupCode:
      properties:
        group_ID:
          type: integer
